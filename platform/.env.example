# OpenAI API key (optional — mock hypotheses used when unset)
OPENAI_API_KEY=

# Default model for hypothesis generation
OPENAI_MODEL=gpt-4o-mini

# Framework data directory (project artifacts)
MIGRATE_PROJECTS_DIR=platform/projects
